// Want to create a select drop down with VIN values from firebase
// Use the Branch ref (VIN) under vin-data ref to populate options
// When select option is submitted, access the required vin ref
// Adapt create-vin-list function an call it within a forEach loop.
// all of this code should be within auth.onAuthStateChanged
// also need to use .on